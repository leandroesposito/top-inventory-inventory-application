<% if (locals.errors) { %>
  <div class="errors">
    <h2>Error</h2>
    <% for(const error of errors) { %>
      <div class="error">
      - <%= error.msg %>
      </div>
    <% } %>
  </div>
<% } %>