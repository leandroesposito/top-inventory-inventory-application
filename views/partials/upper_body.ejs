<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= locals.title ?? "Top Inventory Application" %></title>
</head>
<body>
  <% if (locals.title) { %>
   <h1> <%= locals.title %> </h1>
  <% } %>